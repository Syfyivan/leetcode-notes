/**
 * 数组去重
 *
 * 输入：任意数组（可能含重复的原始值）。
 * 输出：去掉重复项后的新数组，保持首次出现的顺序。
 *
 * ——— 示例 ———
 * 输入： [1, 1, 2, 3, 3, 3, 4]      输出： [1, 2, 3, 4]
 * 输入： ['a', 'b', 'a', 'c']       输出： ['a', 'b', 'c']
 */

function unique() {
}

// —— 自测 ——
console.log(unique([1, 1, 2, 3, 3, 3, 4])); // [1, 2, 3, 4]
console.log(unique(['a', 'b', 'a', 'c']));  // ['a', 'b', 'c']

module.exports = { unique };
