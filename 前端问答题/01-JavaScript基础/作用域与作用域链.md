# 作用域与作用域链

## 核心回答（面试口述版，1 分钟能讲完）

TODO

## 展开

1. 全局作用域 / 函数作用域 / 块级作用域（let/const）的区别
2. 变量提升（Hoisting）的规则 —— var、function 声明、let/const 的不同表现
3. 作用域链查找的方向：从内到外，不是从外到内

## 代码 / 例子

```js
// 经典题：var a = b = 10 各自声明在哪个作用域？
// let 在 for 循环里的「每次迭代独立绑定」
```

## 常见追问

- Q: 什么是暂时性死区（TDZ）？
  A:

## 自己的易错点 / 之前答崩过的地方

TODO
</content>
