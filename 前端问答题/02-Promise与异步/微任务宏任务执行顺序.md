# 微任务与宏任务的执行顺序

## 核心回答（面试口述版，1 分钟能讲完）

TODO

## 展开

1. 宏任务、微任务各有哪些成员（列表背下来）
2. 一个完整循环：调用栈清空 → 所有微任务跑完 → 渲染 → 取一个宏任务
3. 微任务里再注册微任务，会被当前轮次跑完吗？（会）

## 代码 / 例子

```js
// 输出顺序题：混合 setTimeout / setImmediate / Promise.then /
// process.nextTick / async 函数 / queueMicrotask / MutationObserver
```

## 常见追问

- Q: process.nextTick 和 Promise.then 在 Node 里谁更快？
  A:

## 自己的易错点 / 之前答崩过的地方

TODO
</content>
